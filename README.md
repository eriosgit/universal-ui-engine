# Universal UI Engine

Núcleo + adaptadores para exploración y automatización universal de UI (Web, Windows
UIA, SAP GUI…). Ver `Plan_Desarrollo_Universal_UI_v2.md` para la estrategia completa.

`main` se mantiene limpio — todo el trabajo entra por PR desde ramas cortas
(`feat/...`), nunca por commit directo. Ver `CLAUDE.md` para el flujo de trabajo.
